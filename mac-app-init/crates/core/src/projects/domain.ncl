# 프로젝트 스캔/동기화 (NCL 생성)
{
  name = "projects",
  description = "프로젝트 스캔/동기화 (NCL 생성)",
  tags = {
    product = 'auto,
    layer = 'local,
    platform = 'any,
  },
  requires = [],
  provides = ["projects status", "projects sync"],
}
