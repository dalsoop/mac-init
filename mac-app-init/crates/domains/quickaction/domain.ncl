# macOS Finder Quick Actions 관리
{
  name = "quickaction",
  description = "Finder 우클릭 → Services 메뉴 등록/제거",
  tags = {
    product = 'auto,
    layer = 'platform,
    platform = 'macos,
  },
  requires = [],
  provides = ["quickaction list", "quickaction add", "quickaction remove", "quickaction install-defaults"],
}
