# Obsidian 볼트 관리, 플러그인, 동기화
{
  name = "obsidian",
  description = "Obsidian 볼트 관리, 플러그인, 동기화",
  tags = {
    product = 'knowledge,
    layer = 'tool,
    platform = 'macos,
  },
  requires = [],
  provides = ["obsidian status", "obsidian install", "obsidian sync", "obsidian plugin-install", "obsidian plugin-list"],
}
