# VeilKey — LocalVault, VaultCenter 시크릿 관리
{
  name = "veil",
  description = "VeilKey — LocalVault, VaultCenter 시크릿 관리",
  tags = {
    product = 'vault,
    layer = 'tool,
    platform = 'macos,
  },
  requires = [],
  provides = ["veil status", "veil bootstrap", "veil check", "veil start", "veil stop"],
}
