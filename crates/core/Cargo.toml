[package]
name = "mac-host-core"
version = "0.1.0"
edition = "2024"
license = "BUSL-1.1"

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"
serde_json = "1"
regex-lite = "0.1"
