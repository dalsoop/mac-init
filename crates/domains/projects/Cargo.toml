[package]
name = "mac-domain-projects"
version = "0.1.0"
edition = "2024"
license = "BUSL-1.1"

[dependencies]
mac-host-core = { path = "../../core" }
clap = { version = "4", features = ["derive"] }
serde_json = "1"
