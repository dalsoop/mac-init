[package]
name = "mac-domain-sd-backup"
version = "0.1.0"
edition = "2024"
license = "BUSL-1.1"

[dependencies]
clap = { version = "4", features = ["derive"] }
mac-host-core = { path = "../../core" }
serde_json = "1"
