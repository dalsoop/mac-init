# Git worktree 브랜치별 폴더 관리
{
  name = "worktree",
  description = "Git worktree 브랜치별 폴더 관리",
  tags = {
    product = 'auto,
    layer = 'local,
    platform = 'any,
  },
  requires = [],
  provides = ["worktree status", "worktree add", "worktree remove", "worktree clean"],
}
